# Frontend-specific rules

- Use React and TypeScript.
- Keep API calls in typed client or feature services, not directly inside components.
- Handle loading, empty and error states.
- Keep SignalR connection handling in a reusable service layer.
- Do not store access tokens in localStorage unless explicitly required.
