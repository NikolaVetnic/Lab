apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: operations-center-grafana-data
  namespace: operations-center
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
