apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: operations-center-prometheus-data
  namespace: operations-center
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
